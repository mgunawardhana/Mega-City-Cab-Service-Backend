apiVersion: v1
kind: Service
metadata:
  name: mega-city-cab-service-backend
spec:
  selector:
    app: mega-city-cab-service-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer